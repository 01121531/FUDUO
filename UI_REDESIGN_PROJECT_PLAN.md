# 富多店铺智能助手：UI 参考化重构项目方案 V1.0

更新时间：2026-07-23

## 1. 项目说明

本项目是一次**纯 UI 优化**。在不改变任何现有功能、业务能力、API 契约、权限模型和数据逻辑的前提下，参考用户提供的目标 UI，对富多店铺智能助手进行系统化视觉重构。

> 最高级约束：只改变页面如何呈现，不改变系统做什么、何时执行、如何请求、如何计算或如何存储。

本方案解决三个问题：

1. 把参考 UI 转换为可复用的设计令牌、组件规范和页面模板，而不是逐页临摹。
2. 让经营概览、店铺、报表、对话、同步和设置页面形成统一的视觉与交互体系。
3. 在桌面端、平板端和移动端保持信息完整、操作高效和状态清晰。

当前尚未收到参考 UI 图片或 URL。正式视觉实施前必须完成第 4 节的视觉基准确认；在此之前可进行代码盘点和组件边界整理，但不锁定颜色、字体、密度和表面风格。

### 1.1 功能冻结清单

下列行为在整个 UI 优化过程中保持原样：

- 路由地址、URL 参数、页面跳转目标和浏览器历史行为。
- API 地址、请求方法、请求体、响应解析、错误处理和调用时机。
- 登录、TOTP、权限判断、店铺范围和管理员能力。
- 同步任务创建、轮询频率、状态转换、重试和恢复逻辑。
- 店铺筛选、日期筛选、排序、搜索、分页和详情打开行为。
- 报表生成、下载、投递和查看逻辑。
- 对话发送、流式输出、停止、模型选择和工具调用逻辑。
- 富多授权、模型配置、微信接入、成员管理和安全设置流程。
- 所有金额、指标、图表数据、统计口径和数据新鲜度计算。
- 后端、数据库、Worker、OpenClaw、部署和环境配置。

允许调整 JSX 的视觉分组和无业务含义的展示结构，但必须保留原有 Props、事件处理器、表单字段、可访问名称和操作结果。

## 2. 现状基线

### 2.1 已有能力

- Next.js App Router + React + TypeScript。
- Lucide 图标、ECharts 图表、CSS 变量和响应式布局。
- 固定侧边栏、顶部状态栏、移动端底部导航。
- 经营概览、店铺、报表、对话、同步和六个设置页面已实现。
- 加载、空数据、无权限、失败、部分成功、重新授权等业务状态已存在。
- Web 单元测试、Playwright E2E、四视口截图和 Axe 无障碍检查已接入。

### 2.2 当前主要问题

- `apps/web/app/globals.css` 已超过 1,200 行，设计令牌、通用组件和页面样式混合，后续视觉统一成本较高。
- 部分组件依赖页面级 class 和内联样式，视觉规则不易复用。
- 页面虽然遵循同一基础色板，但信息密度、区块层级和操作栏组合仍需要统一。
- 现有视觉是通用运营后台风格，尚未建立与目标参考 UI 的明确映射。

## 3. 目标与边界

### 3.1 项目目标

- 建立一套从参考 UI 提取、可在代码中执行的设计系统。
- 核心页面在第一视口内突出经营数字、数据新鲜度、异常和主要操作。
- 同类组件在所有页面保持一致的尺寸、间距、状态和交互反馈。
- 桌面端适合高频扫描与比较，移动端适合快速查看与单手操作。
- 完整保持现有路由、权限控制、URL 筛选、API 调用、事件处理和业务状态不变。
- 通过自动化测试、视觉截图和真实浏览器检查证明改造没有功能回归。

### 3.2 不在本次范围

- 不新增、删除、合并或改变任何功能。
- 不重写 API、Worker、数据库或 OpenClaw 逻辑。
- 不修改销售、订单、退款和报表计算口径。
- 不修改请求参数、响应结构、轮询时间、缓存规则和错误恢复策略。
- 不改变按钮、筛选器、表单和导航执行后的结果。
- 不以 UI 优化为由顺带修复、重构或扩展业务逻辑；发现功能问题只记录，不在本项目处理。
- 不新增营销首页、宣传 Hero 或与内部工作台无关的装饰页面。
- 不因为视觉改造引入新的前端状态管理框架。
- 不在没有真实需求的情况下整体迁移到另一套组件库。
- 不以桌面端截图为唯一目标，禁止牺牲移动端和可访问性。

## 4. 参考 UI 基准确认

收到参考图片或 URL 后，先输出一份“视觉基准表”，由用户确认后再进入编码。

### 4.1 需要的参考输入

- 至少一张完整桌面端页面截图或可访问 URL。
- 如果参考产品有移动端形态，补充移动端截图。
- 如只参考局部，需要标明重点：导航、数据卡、表格、图表、表单或对话区。
- 需要保留的品牌颜色、Logo、名称和字体限制。

### 4.2 提取维度

| 维度 | 需要确认的内容 | 代码产物 |
| --- | --- | --- |
| 应用框架 | 侧栏、顶栏、内容宽度、导航层级 | Shell 尺寸令牌与布局组件 |
| 视觉密度 | 行高、控件高度、区块间距、信息量 | spacing、size、density 令牌 |
| 色彩 | 背景、表面、主色、语义状态、边框 | primitive 与 semantic color tokens |
| 字体 | 字体族、字号、字重、数字样式 | typography tokens |
| 表面层级 | 边框、阴影、圆角、浮层 | elevation、radius、surface tokens |
| 组件 | 按钮、筛选、表格、卡片、标签、抽屉 | 组件规格与 React 原语 |
| 图表 | 线条、网格、Tooltip、图例、数据标签 | ECharts 主题配置 |
| 动效 | Hover、切换、抽屉、加载、状态变化 | motion tokens 与 reduced-motion 规则 |

### 4.3 参考规则

- 参考其视觉语言和信息组织，不复制品牌资产、专有文案或无关业务结构。
- 参考样式必须适配富多的经营数据、同步状态、授权和微信接入场景。
- 当参考 UI 与可访问性、中文长度或移动端操作冲突时，以可用性为准。
- 所有最终选择落入设计令牌，禁止在页面中散落新的任意颜色和尺寸。

## 5. 设计原则

1. **工作台优先**：页面用于反复查询和处理业务，不做营销式大标题和装饰性首屏。
2. **数据优先**：金额、订单、退款、新鲜度和异常始终高于装饰元素。
3. **状态明确**：状态同时使用图标、文字和颜色，不依赖颜色单独表达。
4. **操作克制**：每个页面只保留一个主要动作，其余操作降级为次要按钮、图标或菜单。
5. **层级稳定**：页面区块使用留白和分隔组织，避免页面区块全部卡片化，也不嵌套卡片。
6. **响应式重排**：移动端重组内容优先级，表格转换为信息行，不简单缩小桌面布局。
7. **无障碍默认开启**：键盘、焦点、对比度、触控尺寸和减少动态效果属于验收条件。

## 6. 目标信息架构

路由和业务分组保持不变：

```text
经营工作
├─ 经营概览        /dashboard
├─ 店铺            /shops、/shops/[shopId]
├─ 报表            /reports、/reports/[reportId]
├─ 对话助手        /chat
└─ 同步中心        /sync

系统设置
├─ 富多授权        /settings/erp
├─ 模型管理        /settings/models
├─ 微信接入        /settings/wechat
├─ 员工与权限      /settings/members
├─ 账号安全        /settings/security
└─ 审计日志        /settings/audit
```

桌面端使用侧边导航；移动端保留最多五项底部导航，其余入口进入“更多”面板。页面标题、面包屑、全局数据状态和用户操作由统一应用框架提供。

## 7. 设计系统建设

### 7.1 令牌层级

```text
Primitive
  原始色阶、字号、间距、圆角、阴影、时长

Semantic
  background、surface、text、border、primary、success、warning、danger、focus

Component
  button、input、table、panel、navigation、drawer、chart、status
```

视觉基准确认后，优先修改令牌，不直接在各页面覆盖样式。现有绿色经营主题可以保留，也可以根据参考 UI 调整，但成功、警告、错误和信息色必须保持明确语义。

### 7.2 核心组件

- 应用框架：`AppShell`、`SidebarNav`、`Topbar`、`MobileNav`。
- 页面结构：`PageHeader`、`PageToolbar`、`SectionHeader`、`ContentPanel`。
- 操作组件：`Button`、`IconButton`、`SegmentedControl`、`FilterMenu`、`SearchField`。
- 数据组件：`Metric`、`DataTable`、`MobileDataRow`、`StatusBadge`、`FreshnessIndicator`。
- 反馈组件：`InlineNotice`、`EmptyState`、`ErrorState`、`Skeleton`、`ProgressState`。
- 浮层组件：`Dialog`、`Drawer`、`DropdownMenu`、`Tooltip`。
- 业务组件：店铺筛选、日期范围、同步时间线、报表状态、授权状态、渠道状态。

只在至少两个页面复用或能够消除明显复杂度时新增抽象，避免为了组件化拆出无业务价值的包装层。

### 7.3 CSS 结构

将现有全局样式按职责逐步拆分，保留一个统一入口：

```text
apps/web/app/globals.css
apps/web/styles/
├─ tokens.css
├─ base.css
├─ shell.css
├─ components.css
├─ data-display.css
├─ forms.css
├─ overlays.css
└─ responsive.css
```

迁移按页面阶段进行，不一次性机械拆分全部 CSS。迁移完成后，组件内不再新增无必要的内联视觉样式。

## 8. 页面改造范围

### 8.1 第一优先级：框架与经营主链路

**应用框架**

- 统一品牌区、导航选中态、顶部状态、用户菜单和移动端导航。
- 明确内容最大宽度、页面边距、滚动行为和固定元素占位。
- Demo/生产标识保持第一视口可见，但不抢占主要业务层级。

**经营概览**

- 重组日期、店铺筛选和立即同步操作。
- 统一 KPI 尺寸、对比信息和数据新鲜度。
- 优化销售趋势、店铺排名、异常和最近同步记录的阅读顺序。
- 保留首屏对核心经营情况的完整判断能力。

**同步中心**

- 强化排队、执行、部分成功、失败和重试的状态层级。
- 桌面端使用可扫描表格，移动端使用任务摘要行。
- 抽屉集中展示阶段时间线、逐店结果、错误恢复和技术详情。

### 8.2 第二优先级：店铺与报表

**店铺列表与详情**

- 统一搜索、状态筛选、排序和店铺行。
- 店铺详情优先展示销售、订单、退款、登录状态和最近同步。
- 图表和明细共享日期、金额和状态格式。

**报表列表与详情**

- 突出报表周期、数据截止、生成状态和投递状态。
- 报表正文采用适合阅读与打印的内容宽度，不套用仪表盘密度。
- 下载、重新生成和查看详情使用清晰的操作优先级。

### 8.3 第三优先级：对话与设置

**对话助手**

- 对话区以内容和工具结果为中心，不使用装饰性聊天气泡堆叠。
- 明确用户消息、助手回答、工具调用、流式生成和失败恢复。
- 输入区在桌面和移动端保持稳定，不被键盘或底部导航遮挡。

**设置页面**

- 富多、模型、微信、员工、安全和审计使用统一设置页模板。
- 当前连接状态与主要恢复动作放在首屏。
- 敏感配置只显示掩码、状态和更新时间，不回显凭证。
- 长表单采用分组和渐进展开，错误显示在对应字段附近。

**登录与账号初始化**

- 保持内部工具定位，直接进入登录或安全设置流程。
- 不增加营销 Hero；移动端输入控件高度不低于 44px。

## 9. 技术实施策略

- 保持 Server Component 与 Client Component 的现有边界。
- 视觉重构不改变 API 请求时机、轮询间隔、缓存语义、权限判断和事件处理器。
- 保持 URL 中的日期、店铺筛选和详情深链接。
- 继续使用 Lucide，禁止新增手绘 SVG 图标和 Emoji 功能图标。
- 继续使用 ECharts，只调整主题、字号、间距、线条和 Tooltip 外观，不改变数据集、聚合和图表含义。
- 先迁移共享组件，再迁移页面；禁止用大量页面覆盖规则修补共享组件。
- 每完成一个阶段立即执行类型检查、单测、生产构建和对应 E2E。
- 每阶段保留可独立回滚的改动边界，提交中不得出现任何业务修复或功能变更。

### 9.1 允许修改的范围

| 范围 | 允许的修改 |
| --- | --- |
| CSS | 颜色、字体、间距、尺寸、边框、阴影、圆角、布局、响应式和动效 |
| 展示组件 | 视觉层级、DOM 展示分组、图标、加载骨架和状态外观 |
| 页面 JSX | 仅调整展示顺序、容器结构和 class，不改变数据获取与事件结果 |
| ECharts | 主题和视觉配置，不改变输入数据、计算与业务 Tooltip 内容 |
| 测试 | 新增视觉截图、布局和无障碍断言；保留全部现有功能断言 |
| 文档 | 设计令牌、组件规范、截图和验收记录 |

### 9.2 禁止修改的范围

以下目录和逻辑不属于 UI 项目：

```text
apps/api/**
apps/worker/**
apps/openclaw-admin/**
packages/**
plugins/**
deploy/**
```

`apps/web` 中的 API 封装、数据转换、状态管理、事件处理、计时器、权限判断和路由参数逻辑同样冻结。若视觉实现必须触碰上述逻辑，停止该项改造并选择不影响功能的 CSS 或展示层方案。

## 10. 实施阶段与工作量

| 阶段 | 工作内容 | 主要交付 | 预估工作量 |
| --- | --- | --- | --- |
| 0. 视觉基准 | 分析参考 UI、现状截图和关键状态 | 视觉基准表、页面映射、确认稿 | 1 人日 |
| 1. 设计系统 | 令牌、字体、间距、通用组件、ECharts 主题 | 设计系统代码与组件状态页 | 2 人日 |
| 2. 应用框架与主链路 | Shell、Dashboard、Sync、响应式导航 | 第一批可运行页面 | 3 人日 |
| 3. 店铺、报表、对话 | 列表、详情、图表、聊天工作区 | 第二批可运行页面 | 3 人日 |
| 4. 设置与认证 | 六个设置页、登录、账号初始化 | 全路由视觉统一 | 2 人日 |
| 5. 全量验收 | 视觉回归、无障碍、功能回归、修复 | 验收截图和报告 | 2 人日 |

总量约 13 人日。该估算以现有业务功能不变、参考 UI 不要求新增复杂交互为前提；视觉基准确认后再锁定最终范围。

## 11. 验证方案

### 11.1 自动化门禁

```bash
pnpm --filter @fuduo/web typecheck
pnpm --filter @fuduo/web test
pnpm --filter @fuduo/web build
pnpm --filter @fuduo/web test:e2e
pnpm verify:release
```

### 11.2 视口矩阵

| 类型 | 建议视口 | 核心检查 |
| --- | --- | --- |
| 手机 | 375 × 812 | 底部导航、抽屉、表单、无横向滚动 |
| 平板 | 768 × 1024 | 导航切换、双列布局、触控尺寸 |
| 桌面 | 1280 × 800 | 第一视口信息密度、固定顶栏、表格 |
| 宽屏 | 1440 × 900 | 内容宽度、图表比例、留白平衡 |

### 11.3 状态矩阵

每个核心页面至少验证：

- 正常数据；
- 初始加载和局部加载；
- 空数据；
- 无权限；
- API 错误和可重试；
- 数据过期或部分成功；
- 长中文、长店铺名、大金额和大量记录；
- 键盘操作、焦点环和 `prefers-reduced-motion`。

### 11.4 视觉验收

- 使用 Playwright 保存全部核心路由的桌面与移动截图。
- 对关键画布和图表执行像素非空检查，防止图表空白。
- 检查文字、徽标、操作栏、抽屉和底部导航无重叠。
- 正文对比度至少 4.5:1，交互目标桌面不低于 36px、移动端不低于 44px。
- 页面不得出现无意横向滚动、布局跳动或动态内容导致的控件位移。

## 12. 完成标准

只有同时满足以下条件，UI 重构才视为完成：

1. 参考 UI 的关键视觉特征已记录并落实到设计令牌和共享组件。
2. 所有现有路由完成统一视觉改造，不存在明显的新旧样式混用。
3. 桌面、平板、手机和宽屏均通过截图检查。
4. 路由、API、权限、数据、状态机、事件处理器和定时行为与改造前完全一致。
5. 现有权限、筛选、同步、报表、聊天、授权和微信流程没有功能回归。
6. 加载、空数据、失败、部分成功、重新授权等状态均有明确界面。
7. TypeScript、全部原有单元测试、生产构建、E2E 和 Axe 检查全部通过。
8. 代码差异只包含 Web 展示层、样式、视觉测试、截图和设计文档。
9. 本地 Demo 在 `http://127.0.0.1:3100` 可完整体验。

## 13. 风险与控制

| 风险 | 控制方式 |
| --- | --- |
| 参考图只覆盖单一页面 | 先提取规则，再为其他页面建立映射，不逐页猜测 |
| 视觉改造影响业务交互 | 冻结事件处理器和数据逻辑，按页面阶段执行全部原有回归测试 |
| 全局 CSS 修改造成连锁回归 | 令牌优先、分阶段迁移、每阶段截图对比 |
| 桌面效果好但移动端失控 | 同步开发四视口，不在最后补移动端 |
| 抽象过度拖慢实施 | 只有复用或明显降低复杂度时新增组件 |
| 参考风格对比度不足 | 保留风格特征，语义色和文字对比按 WCAG 调整 |
| 长中文与真实数据破坏布局 | 使用长文案、大金额和异常状态 Fixture 验证 |

## 14. 交付物

- 参考 UI 视觉基准表。
- 更新后的设计令牌和 ECharts 主题。
- 共享 UI 组件与状态规范。
- 全部业务页面的桌面、平板和移动端实现。
- Playwright 基准截图与无障碍检查结果。
- 更新后的 `UI_UX_DESIGN_PLAN.md` 和 `IMPLEMENTATION_STATUS.md`。
- 可运行的本地 Demo 和最终发布门禁结果。

## 15. 启动条件

进入阶段 0 前需要用户提供参考 UI 图片或 URL。确认视觉基准后，在“只改展示层”的边界内按以下顺序执行：

```text
视觉基准确认
  -> 设计令牌与共享组件
  -> 应用框架
  -> 经营概览与同步中心
  -> 店铺、报表与对话
  -> 设置与认证
  -> 全量视觉和功能验收
```
