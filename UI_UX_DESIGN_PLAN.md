# 富多店铺智能助手：UI/UX 设计方案 V1.0

## 1. 产品定位

这是公司内部反复使用的经营数据工具，不是营销网站。首屏直接进入业务工作台，设计目标按优先级排序：

1. 用户能快速判断销售表现和数据是否新鲜。
2. 用户能用自然语言完成跨店铺查询和对比。
3. 授权、同步、模型和微信连接异常能够被明确发现并恢复。
4. 高频任务减少点击，低频配置逐步展开。
5. 所有金额、比例、时间和状态具有一致的显示规则。

## 2. 用户与场景

### 2.1 V1 用户

- 管理员：配置富多授权、模型、微信和系统参数，可查看全部数据。
- 经营人员：查询店铺经营数据、生成报表、使用对话助手。
- 只读人员：查看被授权的店铺和报表，不可修改系统配置。

V1 实际只有 1 名管理员，但信息架构和权限组件按多员工预留，不在第一版增加复杂组织管理页面。

### 2.2 高频场景

- 查看今天、昨天、近 7 天的全部店铺销售。
- 对比店铺销售额、订单量、客单价和退款情况。
- 从微信私聊询问经营问题。
- 生成并查看日报、周报。
- 发现数据过期、富多授权失效或同步失败。
- 切换默认模型并测试连接。

## 3. 设计原则

### 3.1 内容优先

- 经营数字和异常状态优先于装饰。
- 页面标题保持 24px 左右，不使用超大标题。
- 页面区块使用留白、分隔线和标题组织，不把每个区块做成悬浮卡片。
- 卡片只用于 KPI、重复店铺项、对话工具结果和模态内容。

### 3.2 明确状态

- 所有数据都显示统计日期和最近更新时间。
- `实时`、`最近同步`、`已过期` 不能只靠颜色区分，必须同时显示文字和图标。
- 请求超过 300ms 显示加载反馈，超过 1 秒显示骨架屏或阶段说明。
- 错误信息包含原因和恢复动作，不只显示“请求失败”。

### 3.3 高效操作

- 常用日期使用分段控件：今天、昨天、近 7 天、近 30 天、自定义。
- 店铺筛选支持搜索、多选、全选和最近使用。
- 表格筛选、排序和日期写入 URL，可刷新和分享当前视图。
- 危险操作使用确认对话框；查询操作不增加多余确认。

### 3.4 可访问性

- 正文和背景对比度至少 4.5:1。
- 所有交互可通过键盘完成，并保留清晰焦点环。
- 图表不只依赖颜色，同时使用标签、线型、图例和工具提示。
- 图标按钮提供 `aria-label` 和可见 Tooltip。
- 支持 `prefers-reduced-motion`。

## 4. 信息架构

### 4.1 桌面导航

固定左侧导航宽度 224px：

```text
经营概览
店铺
报表
对话助手
同步中心
────────
设置
  富多授权
  模型管理
  微信接入
  员工与权限
  审计日志
```

顶部栏高度 56px：

- 当前页面标题或面包屑；
- 全局数据新鲜度；
- 通知入口；
- 当前用户菜单。

内容区采用弹性宽度，不用营销站式窄容器。建议：

```text
content padding: 24px
minimum content width: 720px
maximum readable report width: 1440px
```

### 4.2 移动端导航

小于 768px 使用底部导航，最多五项：

```text
概览 | 店铺 | 对话 | 报表 | 更多
```

配置页面收进“更多”。表格在移动端转成字段优先的列表行，不把完整桌面表格横向压缩。

### 4.3 路由

```text
/login
/dashboard
/shops
/shops/[shopId]
/reports
/reports/[reportId]
/chat
/sync
/settings/erp
/settings/models
/settings/wechat
/settings/members
/settings/audit
```

## 5. 视觉方向

### 5.1 风格

采用“现代运营工作台”风格：

- 浅色模式优先，支持深色模式但不作为 V1 阻塞项；
- 中性浅灰页面背景、白色工作表面；
- 主色使用稳健绿色，表达经营正常和数据可用；
- 信息操作使用蓝色；
- 警告使用琥珀色，错误使用红色；
- 阴影克制，主要依赖边框、层级和留白；
- 圆角不超过 8px；
- 不使用渐变球、玻璃拟态、霓虹、插画式空白页。

### 5.2 字体

```css
font-family: Inter, "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
```

- 金额、数量、比例使用 `font-variant-numeric: tabular-nums`。
- 字号固定使用设计令牌，不随 viewport 连续缩放。
- 字间距保持 `0`。

### 5.3 三层设计令牌

#### Primitive

```text
neutral-0       #FFFFFF
neutral-25      #F7F9F8
neutral-50      #EFF3F1
neutral-100     #E2E8E5
neutral-300     #A7B2AC
neutral-600     #53615A
neutral-800     #27332D
neutral-950     #142019

green-50        #EAF7F1
green-100       #CDEBDD
green-600       #137A55
green-700       #0E6345

blue-50         #EDF4FF
blue-600        #246BCE
blue-700        #1D56A6

amber-50        #FFF6E8
amber-600       #B25E09

red-50          #FFF0F0
red-600         #C43D3D
```

#### Semantic

```text
background              neutral-25
surface                 neutral-0
surface-subtle          neutral-50
text-primary            neutral-950
text-secondary          neutral-600
border                  neutral-100
border-strong           neutral-300
primary                 green-600
primary-hover           green-700
info                    blue-600
success                 green-600
warning                 amber-600
danger                  red-600
focus-ring              blue-600
```

#### Component

```text
button-primary-bg       primary
button-primary-hover    primary-hover
input-border            border-strong
table-header-bg         surface-subtle
kpi-positive            success
kpi-negative            danger
stale-banner-bg         amber-50
chat-tool-bg            blue-50
```

### 5.4 间距和尺寸

使用 4px 基础网格：

```text
spacing: 4, 8, 12, 16, 20, 24, 32, 40, 48
radius: 4, 6, 8
icon: 16, 18, 20, 24
control height: 32 compact, 40 default, 44 mobile
sidebar: 224
topbar: 56
mobile bottom nav: 64 + safe area
```

### 5.5 字体层级

```text
page-title       24/32, 650
section-title    18/26, 600
panel-title      15/22, 600
body             14/22, 400
body-strong      14/22, 600
label            13/18, 500
caption          12/18, 400
kpi-value        28/36, 650
```

## 6. 核心组件

### 6.1 按钮

- Primary：每个页面最多一个主要动作。
- Secondary：普通操作。
- Ghost：表格行、工具栏轻量操作。
- Danger：撤销授权、删除配置。
- 图标按钮固定为 36x36，移动端点击区域至少 44x44。
- 使用 Lucide 图标；复制、刷新、下载、筛选等不重复写冗长文字。

状态必须覆盖：default、hover、active、focus-visible、loading、disabled。

### 6.2 状态徽标

统一状态：

```text
正常          绿色圆点 + 文本
同步中        蓝色旋转图标 + 文本
等待重试      蓝色旋转图标 + 文本
待重新授权    琥珀三角 + 文本
失败          红色圆形感叹号 + 文本
数据过期      琥珀时钟 + 文本
未配置        灰色圆点 + 文本
```

徽标只用于紧凑状态，不用作主要按钮。

### 6.3 数据表格

- 表头固定，行高 44px 或 48px。
- 数字列右对齐并使用等宽数字。
- 店铺名称左对齐，状态和更新时间不截断关键内容。
- 默认显示 20 行，支持分页；超过 100 行再考虑虚拟化。
- 排序状态有图标和 `aria-sort`。
- 行操作使用 `MoreHorizontal` 菜单。
- 空表格显示原因、筛选清除和主要恢复动作。

### 6.4 日期和店铺筛选

- 常用日期使用分段控件。
- 自定义日期使用日期范围弹层。
- 店铺使用可搜索多选菜单，显示已选数量。
- 筛选变化后更新 URL；不自动触发高成本实时查询时显示“应用筛选”。

### 6.5 图表

统一使用 ECharts：

- 销售趋势：折线图，最多比较 5 家店铺；超过时切换为汇总线或要求缩小范围。
- 店铺排名：水平条形图，条末端直接显示金额。
- 销售构成：堆叠条形图，不使用难以比较的 3D 饼图。
- 退款趋势：折线或柱线组合图。
- 日期缺失显示断点，不自动补 0。
- Tooltip 显示统计日期、金额、同比和数据更新时间。
- 图表下提供可访问的数据表视图。

### 6.6 金额和比例格式

```text
完整金额：¥12,345.67
紧凑金额：¥12.35万
数量：12,345
比例：12.34%
无数据：—
真实零值：0 或 ¥0.00
```

紧凑金额只用于 KPI 和图表坐标；表格和详情优先显示完整金额。

## 7. 页面规格

### 7.1 登录页 `/login`

第一屏直接提供内部账号登录，不设置品牌营销 Hero。

布局：

- 居中 400px 表单区域；
- 产品名称“富多店铺智能助手”；
- 账号、密码、验证码或 TOTP；
- 主按钮“登录”；
- 后端不可用时在表单上方显示明确状态。

登录错误显示在对应字段附近；提交后按钮进入 loading 并防重复提交。

### 7.2 经营概览 `/dashboard`

顶部：

- 页面标题；
- 日期分段控件；
- 店铺筛选；
- 数据新鲜度；
- “立即同步”按钮。

主要内容顺序：

1. KPI 带：销售额、订单量、付款人数、客单价、退款金额。
2. 销售趋势全宽图表。
3. 店铺排名表和异常提示双栏。
4. 最近同步记录。

KPI 每项固定最小宽度，加载文案、涨跌标签不能改变整体高度。同比为负不必全部用红色，只有对经营不利的变化才使用危险语义。

### 7.3 店铺列表 `/shops`

采用工作表格，不使用店铺卡片瀑布流。

字段：

```text
店铺名称
平台
shopId
accountId
登录状态
今日销售额
今日订单量
退款金额
最近同步
操作
```

支持搜索、登录状态筛选、销售排序。点击店铺名称进入详情，行本身不绑定不明确的点击行为。

### 7.4 店铺详情 `/shops/[shopId]`

页面头部：店铺名称、平台、登录状态、最近同步、立即同步。

标签页：

```text
概览 | 销售 | 订单 | 退款 | 数据状态
```

概览包含 KPI 和最近 30 天趋势；销售、订单、退款页采用图表加明细表；数据状态展示每类数据的最后成功时间和错误。

### 7.5 报表 `/reports`

- 报表列表采用表格：名称、类型、统计范围、生成时间、推送状态、操作。
- “新建定时报表”使用右侧 Drawer，避免离开列表上下文。
- 调度选项使用时间选择器和星期复选框。
- 预览页展示最终微信文本/卡片效果和数据截止时间。

### 7.6 对话助手 `/chat`

桌面布局：

```text
会话列表 260px | 对话主区 minmax(0, 1fr) | 上下文抽屉 320px（按需打开）
```

对话主区：

- 顶部显示当前模型、店铺上下文和数据新鲜度；
- 消息区不放在装饰卡片内；
- 用户和助手消息宽度不超过 760px；
- 输入区固定在底部，支持多行、发送和停止生成；
- 推荐问题只在空会话出现，开始对话后收起。

工具调用展示：

- 默认显示“正在查询 8 家店铺的昨日销售”；
- 完成后显示耗时、数据日期和结果摘要；
- 技术参数和原始 JSON 放在可展开详情中；
- 永不显示 Authorization、Cookie 或模型 API Key。

回答中的经营数据用紧凑表格或列表，不把每个数字都做成独立卡片。

### 7.7 同步中心 `/sync`

页面首部显示：当前状态、队列长度、最后成功时间、失败任务数。

下方包含：

- 按数据类型和店铺筛选的同步记录表；
- 单次同步的阶段时间线；
- 错误原因和恢复操作；
- “重试失败任务”主要操作。

最近 7 天校正以一条组合任务展示，详情按销售、订单、退款和业务日期分组，并显示累计成功、失败数量；部分店铺或部分数据类型失败时标记为“部分成功”，不能被最后一个日期的结果覆盖。

`RETRY_WAIT` 显示为“等待重试”并计入运行中任务，不计入最终异常数；“最后成功”使用任务完成时间，不使用开始时间。任务类型使用中文业务名称，内部队列名称只放在可展开详情中。

### 7.8 富多授权 `/settings/erp`

未授权状态直接显示二维码登录入口，不显示复杂说明墙。

二维码流程状态：

```text
生成中 -> 等待扫码 -> 已扫码待确认 -> 正在验证 -> 授权成功
                                 \-> 已取消/已过期/失败
```

页面元素：

- 220x220 稳定二维码区域，状态变化不能改变容器尺寸；
- 4 分钟倒计时；
- 重新生成按钮；
- 成功后显示账号、店铺数量、Token 到期时间和自动刷新状态；
- 提供“重新授权”和“撤销授权”；
- 兜底入口“使用本地插件同步授权”放在次级区域。

`REAUTH_REQUIRED` 状态必须明确显示“后台同步已暂停、历史数据仍可查询”，并把“重新扫码”作为页面唯一主要操作。微信通知使用相同恢复文案，不显示 Authorization、Token 版本或内部错误详情。

### 7.9 模型管理 `/settings/models`

- 供应商表格：名称、Base URL、默认模型、状态、最近测试、操作。
- 新增/编辑使用 Drawer。
- API Key 输入后只显示掩码，不可重新查看原文。
- 模型角色使用三个 Select：默认对话、复杂分析、备用模型。
- “测试连接”显示耗时、模型名和明确错误。

### 7.10 微信接入 `/settings/wechat`

- 显示 OpenClaw Gateway 状态和微信插件版本。
- 微信扫码区域与富多授权二维码视觉一致，但使用不同标题和状态文案。
- 配对列表包含微信昵称、内部用户、状态、首次配对时间和撤销操作。
- 明确标记当前只支持私聊。

### 7.11 审计日志 `/settings/audit`

表格字段：时间、用户、渠道、动作、店铺、结果、耗时、追踪 ID。

详情 Drawer 只展示脱敏参数。支持按用户、工具、店铺、结果和日期筛选。

## 8. 全局状态体验

### 8.1 加载

- 页面首次加载使用与最终布局同尺寸的骨架屏。
- 小范围刷新保留旧数据并显示轻量进度，不整页闪白。
- 对话流式响应显示停止按钮，不使用无限旋转图标作为唯一反馈。

### 8.2 空状态

空状态区分：

- 尚未授权富多；
- 已授权但没有店铺；
- 当前筛选无结果；
- 尚未同步数据；
- 尚无会话或报表。

每种空状态只提供一个最可能的下一步动作。

### 8.3 错误

错误结构统一为：

```text
标题：发生了什么
说明：对用户的影响
动作：如何恢复
追踪 ID：便于排查
```

授权失效和数据过期使用页面级 Banner；单个店铺失败保持其他店铺数据可用。

### 8.4 数据新鲜度

```text
实时      最近 10 分钟内成功
最近同步  10～60 分钟
已过期    超过 60 分钟或最近同步失败
未知      从未成功同步
```

所有报表和对话答案必须带数据截止时间。

## 9. 响应式规则

断点：

```text
375px   小屏手机验收基线
768px   平板/移动布局切换
1024px  桌面侧栏布局
1440px  宽屏数据布局
```

- KPI：桌面 5 列，平板 2～3 列，手机 2 列；最长文字允许换行但高度固定。
- 图表：手机保持最小 280px 高度，减少同时展示的系列。
- 对话：手机隐藏会话侧栏和上下文抽屉，通过按钮打开 Sheet。
- 表格：手机转换为优先字段列表；不能仅依赖横向滚动。
- 固定顶部栏和底部输入区必须为内容预留空间。

## 10. 动效

- Hover/Press：120～160ms。
- Drawer/Dialog：180～240ms。
- 内容替换：淡入淡出 160ms。
- 只动画 `transform` 和 `opacity`，不动画宽高造成布局抖动。
- 状态变化动画必须可中断。
- `prefers-reduced-motion` 下关闭位移和装饰动画。

## 11. 前端组件映射

优先使用 shadcn/ui：

```text
Button, Input, Label, Select, Checkbox, Switch
Tabs, Tooltip, Popover, DropdownMenu
Dialog, AlertDialog, Sheet
Table, Badge, Skeleton, Progress
Command, Calendar, Date Range Picker
Toast/Sonner, ScrollArea
```

图标统一使用 `lucide-react`。图表使用 ECharts React 封装，图表配置由业务组件生成，不允许页面复制大量临时配置。

## 12. UI 验收标准

- 375、768、1024、1440 四个宽度无横向溢出和内容遮挡。
- 所有交互控件键盘可达，焦点顺序正确。
- 正文对比度达到 WCAG AA。
- 图标按钮均有 Tooltip 和可访问名称。
- 金额、日期、比例和空值格式一致。
- 加载、空、错误、过期、部分成功状态齐全。
- 二维码、图表、表格和对话输入区尺寸稳定，不因异步内容产生明显布局偏移。
- 页面中不出现明文 Token、Cookie、API Key。
- 仪表盘在常规办公屏幕首屏能看到 KPI、趋势图上半部分和异常入口。
- Playwright 对桌面和移动端生成截图并进行人工检查。
